#basic functions for app

def sortByAlphabetical(unsorted_list):
    sorted_list = sorted(unsorted_list)
    return sorted_list


def calculateTotal(unsorted_list):
    total = sum(unsorted_list)
    return total

def equals(x, y):
    return (x==y)
